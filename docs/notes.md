# Notes

Profiling sessions should pin Nim versions in CIâ€”`-d:release` toggles can surprise latency benchmarks.

Pair clamps with explicit newline normalization before hashing transcripts downstream.
